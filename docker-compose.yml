services:
  app:
    image: mcr.microsoft.com/devcontainers/javascript-node:18
    volumes:
      - .:/workspace
    command: sleep infinity
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    env_file:
      - .env
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
