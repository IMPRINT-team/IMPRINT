# IMPRINT

IMPRINT is a distributed RFID activation and interaction platform that treats RFID scans as intentional, authenticated physical inputs. The repository currently contains infrastructure scaffolding for local development.

## Setup
1. Run `./setup/bootstrap.sh`
2. Run `docker compose up -d`
3. Run `./setup/verify_connections.sh` to validate the environment
